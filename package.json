{
  "name": "chatmap-backend",
  "version": "0.1.0",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "start": "NODE_ENV=production node src/server.js",
    "prisma:migrate": "npx prisma migrate deploy",
    "prisma:gen": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "bcryptjs": "^2.4.3",
    "fastify": "^4.28.0",
    "@fastify/cors": "^8.4.0",
    "fastify-jwt": "^7.0.1"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}